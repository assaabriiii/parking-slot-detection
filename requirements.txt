# Parking occupancy MVP — Colab / local deps
# Install: pip install -r requirements.txt
# On Colab Python 3.13, reinstall Pillow AFTER everything else, then restart runtime.

datasets>=2.19.0
huggingface_hub>=0.23
ultralytics>=8.0.0
opencv-python-headless>=4.8.0
numpy>=1.23
matplotlib>=3.7
pandas>=2.0
tabulate>=0.9
tqdm>=4.66
fiftyone>=0.23
pillow==11.2.1
pyyaml>=6.0
pydantic>=2.7,<3
# torch/torchvision come preinstalled on Colab — do not pin torchvision here (PIL/_Ink conflicts)
